{
  "name": "cube-with-athena",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "playground": "yarn && ./node_modules/.bin/cubejs-server dev-server",
    "dashboard": "cd ./dashboard-app && yarn && yarn start"
  },
  "devDependencies": {
    "@cubejs-backend/athena-driver": "^0.23.8",
    "@cubejs-backend/server": "^0.23.8"
  }
}